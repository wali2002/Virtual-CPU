Virtual CPU of an ARM architecture 

From: Sabir Rustaqi

Zip includes:
The source code, the exucutable file, and files that 
are hand assembled to test the virtual CPU.

Note: only when running the memory Dump in the virtual CPU. Press space on 
the command line first and then type in the command that should be executed.

 